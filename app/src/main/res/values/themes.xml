<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoodBook" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/verde_petrolio</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/royal_blue</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#0D4EF2</item>

        <!-- Customize your theme here. -->

    </style>

    <style name="CardView.style" parent="CardView.Light">

        <item name ="cardElevation">10dp</item>
        <item name ="cardCornerRadius">10dp</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MoodBook.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MoodBook.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MoodBook.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>